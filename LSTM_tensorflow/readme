
RNN:递归神经网络，用以解决时间序列问题。但其具有梯度消散的问题。所谓梯度消散，就是说随着信号的传输，很多有用信息将会损失。
但如果利用y = x这种激活函数那么网络确实不会出现梯度消散这种问题，但这种网络不会忘记时间序列上的任何问题，网络不具有选择性，
因此是不合理的。基于上述原因LSTM网络诞生了。
LSTM 网络是一种具有门结构的时间序列网络，常规的LSTM包含输入门、输出门、遗忘门。这三个门是包含大量参数用以决策那些信息是有
用的要被输出，那些信息在输入的时候要被过滤，又有那些信息在传递的过程要被遗忘。这三个门的参数由大量时间序列数据进行训练得到。
